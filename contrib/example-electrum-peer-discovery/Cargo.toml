[package]
name = "electrum_peer_discovery"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio-native-tls = "0.3"
native-tls = "0.2" # Underlying TLS library
